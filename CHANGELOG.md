# Changelog

All notable changes to Vibes to Verified will be recorded here.

## Unreleased

## 0.1.0 - 2026-07-15

- Published the source repository and verified the public `main` CI path.
- Published the approved repository launch post and verified its author, exact
  normalized copy, post ID, and attached 720x1280 video through X API v2.
- Replaced placeholder clone commands with the live public repository URL.
- Pinned stable installation commands to the `v0.1.0` tag while documenting how
  to opt into the changing `main` branch.
- Added Windows PowerShell installation that honors `HERMES_HOME` and falls
  back to the current user's local AppData Hermes directory, while retaining a
  Bash installation path for macOS/Linux.
- Added full-history Gitleaks CI, weekly Dependabot updates, SHA-pinned GitHub
  Actions, and root-scoped local secret ignores that keep suspicious nested
  files visible to Git.
- Added a hard schema-validity gate to `/v2v` final evidence-card output.
- Added regression coverage for the required schema-output instructions after a natural-path invocation returned structurally invalid YAML.
- Added dispatcher/toolset preflight guidance so slash-prefixed prompt text cannot be mistaken for command-resolution proof.
- Restricted Markdown link tests to Git-tracked public files so untracked private production archives cannot contaminate release validation.

- Added the portable Vibes to Verified Agent Skill with the direct `/v2v` command identifier.
- Defined the V0–V4 evidence maturity scale.
- Separated substantive verdict from evidence maturity.
- Added bounded independent approach and refutation workflows.
- Added a machine-readable evidence-card schema and semantic validator.
- Bound promotion evidence to both the main claim and exact final artifact.
- Required structured controlled-test, independent-review, claim-disposition, blocker, natural-runtime, monitoring, recovery, and read-back fields.
- Enforced GREEN consistency while preserving independent verdict and maturity axes.
- Enforced V2/V3/V4 promotion prerequisites in both Draft 2020-12 schema and Python semantics.
- Required separate natural-runtime and outcome-read-back artifacts for V4.
- Added regression fixtures for claim linkage, artifact identity, review independence, GREEN blockers, `GREEN / V0`, `RED / V4`, and incomplete V4 proof.
- Expanded privacy scanning to every tracked text file plus PNG and video metadata.
- Sanitized GPT Image source metadata without changing pixel content.
- Bound the repository and sanitized case-study destinations after public
  read-back while keeping the unpublished X Article URL placeholder-gated.
- Reclassified the public PAPER private-V4 claim as `BLOCKED / V0` because raw primary receipts remain private.
- Replaced a synthetic overclaiming README example with a schema-valid `BLOCKED / V0` fixture.
- Added an explicit validation dependency file and CI cache binding.
- Added coding, research, and sanitized options paper-trading examples.
- Added package, media, link, privacy, and video contract tests.
- Added deterministic SVG/PNG graphics and a GPT Image 2-based Article cover.
- Added a native 9:16 muted Manim overview and a narrated moving-background launch cut.
- Exercised a literal `/v2v` invocation through Hermes Desktop's skill
  dispatcher against installed commit `a88ecf9`. The installed `SKILL.md` hash
  matches this release candidate; Claude Code live invocation remains untested.
- Added the complete draft X Article and launch copy.

The X Article and its separate Article-launch post/replies remain unpublished.
